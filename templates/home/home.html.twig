{% extends 'base.html.twig' %}
{% block title %}Home{% endblock %}

{% block body %}



<h1 class="text-center anonymous-pro-bold my-5">Le meilleur du crochet!</h1>

<h2 class="text-center anonymous-pro-bold my-5">Fait à la main</h2>

 <div class="container my-5">
        <img src="/pictures/Stars.svg" alt="Etoiles" class="img-fluid mx-auto d-block" style="max-width: 80%; height: auto;">

        <!--  LE CAROUSEL  -->

        <h2 class="text-center my-5 anonymous-pro-bold">Découvrez La sélection de la boutique</h2>
  
    <div id="articleCarousel" class="carousel slide" data-bs-ride="carousel">
        <div class="carousel-inner">
            {% for article in randomArticles %}
                <div class="carousel-item {% if loop.first %}active{% endif %}">
                    <div class="d-flex flex-column align-items-center justify-content-center p-4">
                        {% if article.image %}
                            <img src="{{ asset('pictures/' ~ article.image) }}"
                                class="d-block"
                                style="width: 300px; height: auto; border-radius: 15px;"
                                alt="{{ article.libelle }}">
                        {% endif %}
                        <h5 class="mt-3">{{ article.libelle }}</h5>
                        <p class="text-muted">{{ article.prix }} €</p>
                        <a href="{{ path('app_details', {'id': article.id}) }}"
                        class="btn ButtonClass mt-2 anonymous-pro-bold">Voir le produit</a>
                    </div>
                </div>
            {% endfor %}
        </div>

    <!-- Navigation -->
    <button class="carousel-control-prev" type="button" data-bs-target="#articleCarousel" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Précédent</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#articleCarousel" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Suivant</span>
    </button>
</div>


        <!-- LES BESTS SELLERS (factice) -->
        <img class="mx-auto d-block m-5 label" src="/pictures/NosBestSellers.svg" alt="Nos Best Sellers ">
        <section class="row justify-content-center align-items-center gap-4 flex-wrap">
                {% for article in articles %}
                    <div class="category-card-in category-card-anim d-flex justify-content-center h-100 col-12 col-sm-6 col-md-4 col-lg-3">
    <div class="CardArticle p-3 text-center" style="width: 18rem;">
        <img src="{{ asset('pictures/' ~ article.image) }}" 
            alt="{{ article.libelle }}" 
            class="img-fluid rounded d-block mx-auto">
        <p class="anonymous-pro-bold">{{ article.libelle }}</p>
        <p class="anonymous-pro-regular">{{ article.description }}</p>
        <p class="anonymous-pro-regular">{{ article.prix }} €</p>
        <p class="anonymous-pro-regular">{{ article.categorie.libelle }}</p>
        <a href="{{ path('app_details', {'id': article.id}) }}" 
           class="btn ButtonClass anonymous-pro-bold p-3 px-5">Détails</a>
    </div>
</div>

                {% else %}
                    <p class="text-center text-muted">Aucun article trouvé 😢</p>
                {% endfor %}
                            
                            
                            
            
            
            
            
            
        
        </section>
    </div>
{% endblock %}