{% extends 'base.html.twig' %}

{% block title %}Votre Panier{% endblock %}

{% block body %}
<main class="container my-4">
    <div class="row">
        <div class="col-12">
            <h1 class="anonymous-pro-bold mb-4">Votre panier</h1>
        </div>
            {% if panier is empty %}
                           <div class="d-flex flex-column align-items-center justify-content-center text-center my-5 py-5">
                                  <!--  <img src="/images/empty_cart.svg" 
                                        alt="Panier vide" 
                                        style="max-width: 220px; margin-bottom: 30px; opacity: 0.9;">-->
                                    <h2 class="fw-bold text-secondary mb-3 anonymous-pro-regular">
                                        Votre panier est vide 🛒
                                    </h2>

                                    <p class="text-muted mb-4 anonymous-pro-regular">
                                        Il semble que vous n’ayez encore rien ajouté à votre panier...  
                                        Faites un petit tour sur notre boutique !
                                    </p>

                                    <a href="{{ path('app_home') }}" class="btn btn-lg ButtonClass px-4 py-2 rounded-pill shadow-sm anonymous-pro-regular">
                                        Découvrir nos produits
                                    </a>
                        </div>
            {% else %}
                    {% if panier is not empty %}
                        <div class="text-end anonymous-pro-regular">
                            <a href="{{ path('app_clear_panier') }}" class="btn btn-danger">
                                <i class="fa fa-trash"></i> Tout supprimer
                            </a>
                        </div>
                    {% endif %}
                    {% for element in panier %}
                    <div class="col-12 mb-3 p-3 border rounded category-card-in anonymous-pro-regular">
                        <div class="row align-items-center">
                            <!-- Image à gauche -->
                            <div class="col-md-3 col-4 text-center">
                                <img src="{{ asset('pictures/' ~ element.article.image) }}" 
                                    alt="{{ element.article.libelle }}" 
                                    class="img-fluid rounded">
                            </div>

                            <!-- Libellé et prix à droite de l'image -->
                            <div class="col-md-5 col-8">
                                <p class="anonymous-pro-bold mb-1">{{ element.article.libelle }}</p>
                                <p class="mb-0">{{ element.article.prix }} €</p>
                            </div>

                            <!-- Quantité et boutons à droite -->
                            <div class="col-md-4 col-12 mt-3 mt-md-0 d-flex align-items-center justify-content-md-end justify-content-start gap-3">
                                    <!-- Bouton moins -->
                                    <a href="/panier/del/{{ element.article.id }}" 
                                    class="btn btn-lg d-inline-flex align-items-center gap-2 px-3 py-2">
                                        <img src="/pictures/MinusButton.svg" 
                                            alt="Retirer" 
                                            style="width:45px; height:45px">
                                    </a>

                                    <!-- Quantité -->
                                    <p class="mb-0 fs-5">{{ element.quantite }}</p>

                                    <!-- Bouton plus -->
                                    <a href="/panier/add/{{ element.article.id }}" 
                                    class="btn btn-lg d-inline-flex align-items-center gap-2 px-3 py-2">
                                        <img src="/pictures/PlusButton.svg" 
                                            alt="Ajouter" 
                                            style="width:45px; height:45px">
                                    </a>
                            </div>
                        </div>
                    </div>
                    {% endfor %}
                    {% if panier is not empty %}
                            <div class="text-end fw-bold fs-5">
                                Total : {{ total|number_format(2, ',', ' ') }} €
                            </div>
                        {% else %}
                            <p>Votre panier est vide.</p>
                        {% endif %}
                        {% if panier is not empty %}
                            <hr>
                            <div class="text-end mt-3">
                                <a href="{{ path('app_commander') }}" class="btn btn-success">
                                    Commander
                                </a>
                            </div>
                        {% endif %}
            {% endif %}
     </div>
</main>
{% endblock %}

