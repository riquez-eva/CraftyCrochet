{# templates/security/login.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
<div class="d-flex justify-content-center align-items-center anonymous-pro-bold">
    <div class="card shadow-lg border-3 borderConnexion" style=" border-radius: 20px; width: 400px;">
        <div class="card-body p-4">
            <h2 class="text-center mb-4 fw-bold textConnexion">Connexion</h2>

            {% if error %}
                <div class="alert alert-danger text-center">
                    {{ error.messageKey|trans(error.messageData, 'security') }}
                </div>
            {% endif %}

            <form action="{{ path('app_login') }}" method="post">
                <div class="mb-3">
                    <label for="email" class="form-label fw-semibold">Email</label>
                    <input type="email" id="email" name="email"
                           value="{{ last_username }}"
                           class="form-control borderConnexion rounded-pill"
                           placeholder="Entrez votre email"
                           required autofocus>
                </div>

                <div class="mb-4">
                    <label for="password" class="form-label fw-semibold">Mot de passe</label>
                    <input type="password" id="password" name="password"
                           class="form-control borderConnexion rounded-pill"
                           placeholder="Votre mot de passe"
                           required>
                </div>

                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                <div class="d-grid">
                    <button type="submit" class="btn btn-orange rounded-pill fw-semibold py-2">
                        Se connecter
                    </button>
                </div>
            </form>

            <p class="text-center mt-3 mb-0">
                Pas encore de compte ?
                <a href="{{ path('app_register') }}" class="textRegister fw-semibold text-decoration-none">
                    Inscrivez-vous
                </a>
            </p>
        </div>
    </div>

</div>

<div class="anonymous-pro-bold text-center my-5">
<h4>Mot de passe oublié?</h4>
<a href="{{ path('app_forgot_password_request') }}" class="btn ButtonClass">CLiquez ici</a>
</div>

<style>
    .text-orange {
        color: orange;
    }
    .border-orange {
        border: 2px solid orange;
    }
    .btn-orange {
        background-color: orange;
        color: white;
        transition: all 0.3s ease;
    }
    .btn-orange:hover {
        background-color: darkorange;
        color: #fff;
    }
</style>
{% endblock %}